---
name: browser-flows
description: Browser automation flows for the symbrowse test harness (flows/ layout)
license: Apache-2.0
metadata:
  source: fixture
---

# Browser Flows

Executable browser-automation flows consumed by `symbrowse flow list`.
The flow documents live in `flows/` and are ordinary skill resources:
they travel through import, render, and install like any other support
file, no executable bit required.
